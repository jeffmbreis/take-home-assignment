import styled from 'styled-components';

const AppWrapper = styled.div`
  display: flex;
  justify-content: center;
`;

const Content = styled.div`
  width: 980px;
`;

const Title = styled.h1`
  color: yellow;
  font-size: 40px;
`;

const styles = { AppWrapper, Content, Title };

export default styles;
